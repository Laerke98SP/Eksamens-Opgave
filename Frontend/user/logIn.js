

function correctLogin(){
    var email = document.getElementById("email");
    var password = document.getElementById("password");

    // call the function that gets user info
    // check if email and passwords match with user

}